name: Deploy Node App

on:
  push:
    branches:
      - main
      - develop

jobs:
  ci-cd:
    # runs-on: ${{ github.ref == 'main' && 'macbook-pro' || github.ref == 'develop' && 'macmini' }}
    runs-on: macmini

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and run Docker container
        run: |
          docker-compose up --build -d
