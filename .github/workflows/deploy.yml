name: Deploy Node App 

on:
  push:
    branches: [main]

jobs:
  build-and-run:
    runs-on: node-github-actions

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and run Docker container
        run: |
          docker-compose up --build -d
